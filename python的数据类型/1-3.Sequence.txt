4.序列
即内容连续、有序，可以使用下标索引的一类数据容器，字符串、元组、列表均可以视为序列
序列的切片操作：从一个序列中取出一个子序列
    序列[起始下标：结束下标：步长] //起始下标默认从头开始，可不写；结束下标默认为结尾，可不写；不包含结束下标；步长默认为1，可以省略不写
    myTuple = (0, 1, 2, 3, 4)
    myTuple[:] //(0, 1, 2, 3, 4)